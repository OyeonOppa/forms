# 📋 คู่มือติดตั้ง — ระบบใบสมัครประกวดสื่อ "เยาวชนรุ่นใหม่ใส่ใจสิทธิชุมชน"

## 📂 โครงสร้างไฟล์

```
project/
├── index.html              ← ฟอร์มสมัคร (Frontend)
├── admin/
│   └── index.html          ← หน้า Admin ดูข้อมูล
├── gas-backend.gs          ← Google Apps Script (Backend)
└── README.md               ← คู่มือนี้
```

---

## 🔧 ขั้นตอนการติดตั้ง

### ขั้นตอนที่ 1: สร้าง Google Sheets

1. ไปที่ [Google Sheets](https://sheets.google.com) → สร้าง Spreadsheet ใหม่
2. ตั้งชื่อ เช่น `ใบสมัครประกวดสื่อ Young & Smart Citizen`
3. จด **Spreadsheet ID** จาก URL:
   ```
   https://docs.google.com/spreadsheets/d/[SPREADSHEET_ID]/edit
   ```

### ขั้นตอนที่ 2: สร้าง Google Drive Folder

1. ไปที่ [Google Drive](https://drive.google.com) → สร้างโฟลเดอร์ใหม่
2. ตั้งชื่อ เช่น `ไฟล์ผลงานประกวดสื่อ`
3. จด **Folder ID** จาก URL:
   ```
   https://drive.google.com/drive/folders/[FOLDER_ID]
   ```

### ขั้นตอนที่ 3: ตั้งค่า Google Apps Script

1. ใน Google Sheets → ไปที่ **Extensions → Apps Script**
2. ลบโค้ดเก่าทั้งหมด → วางโค้ดจากไฟล์ `gas-backend.gs`
3. แก้ไข 2 ค่านี้:
   ```javascript
   const SPREADSHEET_ID = 'ใส่ Spreadsheet ID จริง';
   const FOLDER_ID = 'ใส่ Folder ID จริง';
   ```
4. กด **Save** (Ctrl+S)
5. เลือกฟังก์ชัน `setupSheet` แล้วกด **Run** ▶️ (รันครั้งเดียวเพื่อสร้าง Header)
   - ครั้งแรกจะขอ Permission → กด Review Permissions → เลือก Account → Allow

### ขั้นตอนที่ 4: Deploy เป็น Web App

1. กด **Deploy → New deployment**
2. ⚙️ Settings:
   - **Type:** Web app
   - **Execute as:** Me (ตัวคุณเอง)
   - **Who has access:** Anyone
3. กด **Deploy**
4. จะได้ **Web App URL** เช่น:
   ```
   https://script.google.com/macros/s/AKfycb.../exec
   ```
5. คัดลอก URL นี้ไว้

### ขั้นตอนที่ 5: ใส่ URL ใน Frontend

1. เปิดไฟล์ `index.html` → หาบรรทัด:
   ```javascript
   GAS_URL: 'YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL'
   ```
   เปลี่ยนเป็น URL จริง

2. เปิดไฟล์ `admin/index.html` → หาบรรทัด:
   ```javascript
   GAS_URL: 'YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL'
   ```
   เปลี่ยนเป็น URL เดียวกัน

### ขั้นตอนที่ 6: ใส่ QR Code ไลน์กลุ่ม

1. สร้างกลุ่ม LINE สำหรับกิจกรรม
2. บันทึก QR Code เป็นรูปภาพ
3. ใน `index.html` → หา `id="qrPlaceholder"` → แทนที่ด้วย:
   ```html
   <div class="qr-placeholder">
       <img src="your-line-qr-code.png" alt="QR Code ไลน์กลุ่ม">
   </div>
   ```

### ขั้นตอนที่ 7: Deploy ขึ้น Cloudflare Pages

1. ไปที่ [Cloudflare Pages](https://pages.cloudflare.com)
2. สร้าง Project ใหม่ → Direct Upload
3. อัปโหลดโฟลเดอร์ `project/` ทั้งหมด
4. Deploy!

---

## 📌 หมายเหตุสำคัญ

### เรื่อง `no-cors` Mode

ฟอร์มส่งข้อมูลแบบ `mode: 'no-cors'` เพราะ Google Apps Script ไม่รองรับ CORS preflight จาก custom domain ข้อจำกัดคือ **ไม่สามารถอ่าน response จาก server ได้** ดังนั้น:
- ถ้าส่งสำเร็จ → แสดงหน้าสำเร็จ
- ถ้า error เกิดที่ฝั่ง GAS → ต้องเช็คจาก Google Sheets / Apps Script Log

### ทางเลือก: ใช้ JSONP-style callback

ถ้าต้องการอ่าน response ได้ ให้เปลี่ยนเป็นส่งผ่าน URL params + redirect:
```javascript
// แทนที่ fetch ด้วย form submit
const form = document.createElement('form');
form.method = 'POST';
form.action = CONFIG.GAS_URL;
// ... append hidden inputs
```

### ไฟล์ที่รองรับ

| ประเภท | นามสกุล | ขนาดสูงสุด | เหมาะกับ |
|--------|---------|-----------|---------|
| ภาพ JPEG | .jpg, .jpeg | 10MB | โปสเตอร์, ภาพนิ่ง |
| ภาพ PNG | .png | 10MB | กราฟิก, โปสเตอร์ |
| เอกสาร PDF | .pdf | 10MB | โปสเตอร์คุณภาพสูง |

### Admin Dashboard

- เข้าถึงได้ที่ `yourdomain.com/admin/`
- **แนะนำ:** ป้องกันหน้า admin ด้วย Cloudflare Access หรือ Basic Auth
- รองรับ: ดูรายการ, ค้นหา, กรองตามประเภทสื่อ, ดูรายละเอียด, ส่งออก CSV

---

## 🔒 ป้องกันหน้า Admin (แนะนำ)

### วิธีที่ 1: Cloudflare Access (แนะนำ)
1. Cloudflare Dashboard → Zero Trust → Access → Applications
2. Add Application → Self-hosted
3. กำหนด path: `yourdomain.com/admin/*`
4. เลือก Policy: Email เฉพาะแอดมิน

### วิธีที่ 2: ซ่อน URL
- เปลี่ยนชื่อโฟลเดอร์ admin เป็นชื่อลับ เช่น `admin-kpi-2568/`

---

## ⚡ Tips

- **ทดสอบก่อน Deploy:** เปิด `index.html` ใน browser ได้เลย ทดสอบ UI
- **ดู Log GAS:** Apps Script → Executions เพื่อดู error
- **แก้ไข Deploy:** ทุกครั้งที่แก้โค้ด GAS ต้อง Deploy → Manage deployments → Edit → New version
